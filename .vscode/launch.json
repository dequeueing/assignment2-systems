{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "uv run Python File",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "python": "${workspaceFolder}/.venv/bin/python",
            "preLaunchTask": "uv sync (check deps)"
        }
    ]
}
